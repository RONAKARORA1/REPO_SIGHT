{
  "name": "repo-sight-vercel",          
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:analyser": "apt-get update && apt-get install -y --no-install-recommends cmake make g++ binutils && mkdir -p frontend/backend/bin && cd analyzer && mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make -j && cp cma ../../frontend/backend/bin/linux-x64-cma"
    "install:backend": "cd backend && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
