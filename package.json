{
  "name": "repo-sight-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "install:backend": "cd backend && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
