{
  "name": "repo-sight-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "apt-get update && apt-get install -y cmake make && mkdir -p frontend/backend/bin && cd analyzer && mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make -j && strip cma && cp cma ../../frontend/backend/bin/linux-x64-cma",
    "install:backend": "cd backend && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
