/**
 * frontend/js/app.js
 * 
 * Placeholder file – the core dashboard logic is implemented in
 * `dashboard.js`, which is loaded by `index.html`.  You can add
 * any application‑specific initialization here if needed.
 */

// Example: log that the app has loaded (remove in production if desired)
console.info('CMA Dashboard app.js loaded');
